# Agent Notes

This file is for future coding agents working in this repository.

## Working Preferences

- Launch `sm.exe` when it is useful for testing the current change. Place the game on monitor 2 for now when practical.
- Prefer preserving the original game feel where possible, while allowing intentional behavior changes for PC-native features.

## Current High-Level Goals

- Analog movement support is in progress.
- `sm` is now the Super Metroid host/test project guiding BlocksBox development. The long-term target is for runtime assets and level data to load from the BlocksBox dump instead of direct ROM reads, with direct ROM access kept only as fallback/verification while parity work is incomplete.
- Left stick movement should feel smooth and intentional, with eased-in walking and full-tilt top speed.
- The physical d-pad should remain available for menus, but not drive Samus movement during active gameplay.
- The on-screen analog debug overlay should be low-noise: keep the aim-direction arrow in the bottom-right and avoid showing raw left-stick numeric values unless specifically requested.
- Right-stick aiming and left-stick movement are intended to be decoupled; left stick only supplies fallback aim when the right stick is idle.
- `Samus_IsAiming()` should represent active right-stick aiming; left stick fallback aim should not block grounded walking transitions.
- Landing into a run and mid-run direction flips should stay responsive even while right-stick aim is active.
- Right-stick aiming must not synthesize left/right/up/down input for movement or pose-transition lookup; aim poses should be applied through the analog pose override path.
- Near-vertical right-stick aim should preserve current facing until horizontal aim crosses the flip threshold, to avoid rapid left/right pose flicker.
- When left-stick movement and right-stick aim are horizontally opposed beyond the flip threshold, analog pose override should force moonwalk instead of allowing vanilla ground turn-around transitions to flicker.
- Moonwalk should end as soon as right-stick aiming stops; left-stick fallback aim should not keep moonwalk active.
- Held jump should not retrigger jumps during analog-driven facing flips; moonwalk jumps should require a fresh jump press.
- Analog moonwalk should clear extra run/speed-boost velocity so moonwalk speed is governed consistently by moonwalk movement and stick tilt, not inherited running momentum.
- Moonwalk's actual horizontal speed cap is being raised to 1.0, while animation cadence still uses the vanilla moonwalk reference speed so full-speed moonwalk animates about twice as fast.
- Walking/running/moonwalking animation cadence should be governed by Samus's horizontal speed divided by the relevant state reference speed, not directly by stick magnitude. Running/walking should not scale above vanilla full-speed cadence because vanilla already has extra run/speed-booster animation handling; moonwalk may scale up to 2x.
- Compatible run/aim-run pose changes and compatible moonwalk/aim-moonwalk pose changes should preserve the current animation frame to avoid gait resets while aiming.
- Moonwalk should show supplemental foot dust while Samus is moving even if compatible pose-frame preservation delays the next normal footstep frame.
- Analog moonwalk forcing should only apply on ground running/moonwalk/turn-around states; airborne opposed-stick aiming should use normal jump/fall aim poses.
- Jumping from moonwalk should enter the non-spinning jump transition poses rather than spin jump or turn-jump.
- Right-stick analog pose selection should include the vanilla straight-down jump/fall aim poses, preserve facing for pure vertical aim, and break spin jump into normal jump aim poses when active.
- Projectile heading/inherited-velocity physics looked good in the latest user test; keep tuning focused on movement and pose interaction unless new projectile issues appear.
- Reference-behavior mismatch checking has been intentionally disabled while analog movement work is underway.
- When `g_skip_menu` is enabled, slot 1 loads directly and receives the all-items debug loadout if anything is missing.
- Debug skip-menu loadout should equip everything except Spazer, which should remain turned off even if other beams/items are granted.
- The PC-native main menu should appear as an overlay on the baby Metroid capsule/title scene, not after changing to file select. `Play` proceeds to the save-slot screen, vanilla file-select Exit returns to the native main menu, `Options` enters the existing options menu, `Test` currently launches the old skip-menu debug loadout until a proper debug scenario is specified, and `Exit` gracefully closes the app.
- Missiles/super missiles are intended to be a direct-fire right-bumper action. Keep their HUD icons visible, skip them in the Select-button cycle, keep one selected once acquired, and use physical d-pad Up to toggle missile versus super missile.
- Right trigger should always fire/charge the beam even when missile/super missile is selected. Missile firing is blocked while beam charge is active, and a missile/super missile shot briefly locks out beam firing for 75% of that projectile's own refire cooldown.
- Right stick click should fire/hold grapple directly without selecting it. Right bumper should fire power bombs while morphed and keep its missile/super missile behavior while non-morphed.
- Analog-heading wave/plasma beams must still run off-screen cleanup so projectile slots are released instead of blocking future shots.
- Shinespark direction should resolve from analog input at the end of windup: right stick when actively aiming, otherwise left stick movement, with no input defaulting to vertical.
- Downward shinesparks reuse the vertical/diagonal-up shinespark poses with a runtime vertical spritemap flip and inverted Y movement rather than adding new pose IDs or art.
- Downward shinespark sprite transforms should mirror/rotate Samus top and bottom spritemaps against one shared signed bounding box and use each OAM entry's actual 8/16px dimensions; flipping each half independently or treating offsets as unsigned garbles downward diagonals.
- Straight-down shinesparks use the fixed vertical flip path; downward diagonals should use the up-diagonal poses with rotated spritemap composition, clockwise for down-right and counterclockwise for down-left.
- 90-degree Samus spritemap rotation must split 16x16 OAM entries into four 8x8 entries and rotate the subtile positions too; otherwise downward diagonal shinesparks appear as misplaced 2x2 chunks.
- Downward diagonal shinespark launches currently request a one-frame BMP screenshot in `debug_screenshots/` to help diagnose the remaining rotated spritemap composition issue.
- Current PPU rotation attempt: downward diagonal shinesparks emit the original up-diagonal Samus OAM range, then the software PPU inverse-maps those sprite pixels around a shared Samus transform center so 8x8 tile pixels rotate instead of only tile boxes.
- Right-stick aim must not keep Samus in a running pose after left-stick movement is released; while aiming, running/moonwalking aim poses should fall back to standing aim poses when there is no horizontal movement input.
- Shinespark windup should last 60 frames by default; releasing jump during windup should launch immediately.
- Shinespark launch direction should be cached during windup from the latest meaningful right-stick aim input, or left-stick fallback input if the right stick is idle; if input returns to neutral before launch, use the last cached direction rather than falling back to facing direction.
- During shinespark windup, keep the normal Samus input handler disabled so analog/directional input updates the cached launch vector without triggering the vanilla windup pose transition table early.
- With shine charge stored, pressing jump while airborne in non-spinning jump/fall states should enter shinespark windup even if Samus is not aiming upward; this path must assign the windup pose immediately rather than only queuing `samus_new_pose`, but must not call `SamusFunc_F433()` on the windup pose because the shinespark movement-type hook only has handlers for launched spark poses.
- Horizontal shinesparks that impact a slope, and downward diagonal shinesparks that impact the ground, should still fire impact effects but should transfer immediately into a charged speedbooster run rather than freezing Samus in the crash handler.
- Morph ball physics are being rewritten toward Sonic-like rolling: right-stick aim should have no effect on morph/springball physics, left/right analog input applies torque, roll velocity should conserve through ground/air/bounce states, flat-ground friction should be gentle, and slopes should accelerate the ball downhill without player input.
- Expanded ability/world-interaction wishlist is being explored for future PC-native mechanics: deeper grapple/enemy interactions such as ripping enemy parts, analog-flick slams, dragging Botwoon from its hole, and pulling the Metroid capsule from Ridley's hands; an X-Ray-style flashlight/scanner; additional missile/projectile families such as cluster, ricochet, lock-on, and tracking shots; water-running with Speed Booster; Dread-style slide kick; Boost Ball; a Janemba-inspired breakdown/reassemble dodge; a melee cutting weapon inspired by Metal Gear Rising; gravity-manipulation powers; orthogonal/background doorway transitions that let rooms branch into depth; more tactile interactables such as levers, buttons, terminals, valves, corpses, containers, and item-retrieval animations.
- Non-ability wishlist items include dynamic camera zoom in/out, room streaming that loads and stitches linked rooms after shooting doors by matching doorway connections, arctic/ice areas, shootable stalactites that fall and crush enemies, ice stalactite chunks that can freeze into temporary platforms on water, buoyant water objects, lightweight drain/fill fluid simulation, and more complex multi-phase boss fights.
- Renderer modernization is starting on branch `modernLayerRenderer`: the first target is to split the flattened SNES software PPU output into ordered priority-band layers, with transparent native custom insertion slots behind all SNES layers, above all SNES layers, and between every adjacent SNES priority band.
- Keep the legacy flattened renderer available as a fallback while the modern layer path gains parity; color math/subscreen behavior is especially sensitive and should be validated carefully as the layer compositor matures.
- Modern layer controls: `Shift+M` toggles the modern layer renderer at runtime and `Ctrl+M` toggles custom-layer debug stripes. The aim indicator/opening-skip prompt should be rendered through the front custom layer when the modern layer renderer is active, not painted directly over the final framebuffer.
- On branch `levelRender`, L3 toggles the native level renderer A/B mode. The native path currently disables the SNES backdrop priority band and emits SNES-equivalent native backdrop/BG1/BG2 pixels into custom slots with palette/layer metadata so the compositor can apply normal color math; the bottom-right overlay shows `SNES` or `NATIVE` next to the aim indicator.
- Native BG1/BG2 now replace the stock SNES BG priority slots in Native mode rather than overlaying them with fallback. If level geometry disappears in Native mode, that indicates a true native coverage gap to fix instead of a hidden stock-renderer fallback.
- Analog projectile visuals are being rotated in the software PPU by registering each projectile's OAM range with its own per-frame transform. The rotation should be the delta between the projectile's true analog heading and the nearest vanilla 8-way projectile art direction.
- Desktop launches should default to borderless fullscreen, but gameplay/rendering and the software PPU buffer are currently forced back to the vanilla 256-wide 4:3 view because the first 16:9 rendering pass caused level graphics artifacts. The leftover six-column widened room streamer in `sm_80.c` caused Ceres left-edge vertical bands and has been reverted to vanilla 4:3 streaming.
- Modern presentation target: the game window/backbuffer should run at modern desktop resolutions while the game world is scaled by an even integer factor to preserve crisp pixel art and stable SNES-world coordinates. Non-integer scaling should be reserved for UI/compositor effects, not the core world view.
- The options menu should keep showing the aspect selector as a placeholder; selecting it currently writes `Widescreen16x9 = 0` and leaves rendering/gameplay in 4:3 until the next 16:9 pass.
- `Screenshot = F12` captures the current presented framebuffer to `debug_screenshots/manual_####.bmp` for renderer debugging.
- Room loads currently queue an automatic delayed screenshot to `debug_screenshots/room_load_####.bmp` so left-edge rendering artifacts can be diagnosed from captured frames.
- Native main menu activation currently queues an automatic delayed screenshot to `debug_screenshots/main_menu_####.bmp`; the preferred presentation is the near-stock title scene with only the native menu box drawn on top.
- The vanilla options screen now has a native volume slider overlay on its main page; left/right adjusts the current runtime app volume in 5% steps.
- The native options overlay is being reorganized into Controls, Video, and Audio zones. Master volume is the only wired audio slider for now; BGM/SFX sliders are visible placeholders, and the video zone has a visible 16:9/4:3 selector placeholder.
- Native options currently persist to `sm.user.ini` by writing `!include sm.ini` plus overrides for `Widescreen16x9` and `MSUVolume`; add future native option values there as they become real controls.
- BlockBox integration is active on branch `BlockBoxIntegration`: the `BlockBox/` submodule is branched to `SuperMetroid`, read-only Super Metroid room/header/state/level-data extraction now works there, startup bootstrap now emits a compact runtime manifest plus room/tileset/art/object indexes, tileset preview images, and assembled Samus frame exports, and the next phase is teaching room/graphics loading paths to consume those exported assets directly while preserving enough metadata to support future replacement graphics packs keyed against the dumped tileset/art/object manifests.
- BlocksBox dump direction: favor functional/editable level data over perfect SNES fidelity. Level placements should identify pack/blockset/block, use transform position/scale/rotation with Super Metroid flip bits converted to scale `-1`, and carry Super Metroid specifics in generic extra data. Blocksets should emit palette-nested per-block PNGs for easy reskin/upscale workflows while preserving source metadata such as quadrant tile words/priority bits for future fidelity.
- BlocksBox object direction: `ObjectAsset`/`ObjectPlacement` are convenience units for editors/runtimes, not strict mirrors of the original game's object taxonomy. Super Metroid title-scene props such as the baby capsule and corpse sprites can be stitched from Mode 7 tiles into generic object sprites, and static room PLMs are dumped as object placements with PLM header, room argument, and x/y block coordinates preserved in `ExtraData`. The first corpse object is 40x8 using title Mode 7 tile row `0xD0-0xD4`; the second uses `0xE4-0xE7` and `0xF4-0xF7`; the third is 32x16 with top knee tile `0xD5` in the third top-row slot over bottom strip tiles `0x75`, `0x65`, `0x7A`, and `0x8A`.
- BlocksBox door direction: canonical blue-door opening/closing `ObjectAsset`s still come from Super Metroid PLM header `0xC8A2` and are ROM-decoded through duration/draw-instruction frame pairs. Variants `0xC8AE/0xC8B4` produced bad horizontal frames, and direct red/green/yellow/grey animation attempts produced garbled art because their palette/tileset/source path is not correctly traced yet. SMILE (`other/SMILE/smile`) is now used as a local Super Metroid editor/reference source: its `files/PLM/*.txt` notes classify grey doors, colored door shells, gates, and eye-door pieces, and its matching PLM GIFs are dumped as static reference frames that override generic `super-metroid.plm-xxxx` placeholders. Door animations and static door references are all nested under `objects/doors/...`; object folders inside `out/super-metroid/objects` should omit redundant `super-metroid` prefixes.
- The PC-native main menu now has a prototype `Level Editor` browser path: it opens a BlocksBox pack list, currently with `Super Metroid`, then a pack page with `Level List` and `Tile Sets`; `Tile Sets` browses imported Super Metroid tilesets and shows exported preview images on hover. The first crash on selection was caused by BOM-prefixed TSV headers being parsed as data; TSV readers should continue to tolerate BOM/comment/header rows and missing fields.
- BlocksBox tileset previews should render each tile once into the smallest near-square power-of-two grid that fits, up to 64x64 cells, leaving unused cells black instead of repeating tiles.
- BlocksBox now exports level preview BMPs for each Super Metroid room state by drawing the optional background tile layer first and the foreground tile layer on top, using the room dimensions in 16x16 blocks and the state's selected tileset. Future level preview work should add optional BTS/collision and scroll-screen overlays.
- Cinematic export work is active on branch `cinematics`. The chosen video target is Matroska `.mkv` with FFV1/FLAC archival output plus a H.264/AAC playback MKV. BlocksBox currently has a proof/comparison capture path: `sm.exe --capture-cinematic-frames <frames.rgb24> --capture-cinematic-audio <audio.wav> --capture-frames <n> --capture-quit <rom>` plays the real opening/title runtime, suppresses native overlays during capture, records full 256x224 RGB24 frames, and captures deterministic 44.1 kHz stereo SPC/DSP audio. The exporter then replaces frames after the zoomed baby-cylinder shot so Nintendo Presents, Metroid 3, Super Metroid logo, and menus do not appear while the captured music/SFX continue, splits `frames.rgb24` into `frames/frame-######.png`, and muxes MKVs. `export-super-metroid-cinematic --source bb` now forces a no-host frame path: it renders the title-sequence `NINTENDO` logo and the interstitial black-cut cards `1994`, `NINTENDO`, `PRESENTS`, and `METROID 3` with editable ASS subtitles between BB-native title Mode 7 camera stages through a small title runtime that owns the no-rotation scale/scroll state and steps the baby Metroid Mode 7 object instruction timer, with no Super Metroid logo/menu cards, then switches to the BB-held baby-cylinder renderer; audio uses an experimental BB-owned helper when `BLOCKSBOX_SM_TITLE_AUDIO_RENDERER` points at `sm_title_audio_renderer.exe`, otherwise it falls back to silence. The helper source is `BlockBox/tools/sm_title_audio_renderer.c`; it uploads the title music bank from the ROM into the lightweight `spc_player`/DSP code, writes title track `0x05` to port 0, mirrors the baby Mode 7 palette script, queues baby Metroid cry SFX `0x23/0x26/0x27` on a deterministic subset of green-flash entries, attenuates the SFX channels so cries stay under the music, and renders 44.1 kHz stereo PCM frame-by-frame. The exporter writes `plan/title-audio-events.tsv`, `plan/title-subtitles.tsv`, `plan/title-chapters.ffmetadata`, `title-subtitles.ass`, and manifest audio/subtitle/chapter entries for the known title music load/track commands, current baby cry SFX cue IDs, the title-card cue timings, and the Matroska chapter marker `looping portion starts here` at frame 1893 / 31.498126s; next audio work is comparing SFX levels/timing to the host-captured oracle WAV. The held section is BB-owned for video composition: it renders dumped title Mode 7 tiles/tilemap through a 1024px Mode 7 register sampler and applies a simple blue title grade. It does not overlay a separate BB-native baby/capsule sprite because the title Mode 7 scene already contains the cylinder; the baby Metroid animation now comes from applying the runtime Mode 7 tile transfer pages into tiles `E0-E3`, with ROM-backed baby palette swaps from script opcodes `$8B:A273/$A284/$A295/$A2A6` applying the four cinematic palettes at `$8B:A2B7/$A2D7/$A2F7/$A317` to CGRAM entries 48-63. The zoom ramp no longer draws an extra manual baby/capsule over the title Mode 7 scene. The title runtime now covers the visible horizontal/vertical pan stages, final scale ramp, and the baby Mode 7 object's timer/goto/set-timer/palette-swap loop, but broader cinematic sprite/OAM rendering, general queued Mode 7 VRAM transfers, and color math/gradient objects are not ported yet. Latest full host-oracle test export: `BlockBox/out/cinematic-title-full-audio-loop/super-metroid`, 7200 frames, with late frames verified on the baby cylinder; latest BB-only smoke export: `BlockBox/out/cinematic-bb-title-runtime/super-metroid`, 2400 frames, with title music audio and default ASS subtitle stream muxed into both MKVs; local reference video for comparison is `BlockBox/out/reference/super-metroid-title-ref.mp4`, with dense per-frame metrics in `BlockBox/out/reference/frame-diff-raw.tsv` and `frame-diff-raw-summary.txt`. The current BB path has monitor green-pixel flicker, black-cut/card timing from dense frame comparison, and camera segments aligned to the reference at frames 473-687, 788-935, and 1036-1251 before the final zoom/hold begins at frame 1436. This must not remain a BlocksBox dependency because BlocksBox will be linked by projects other than `sm`; port only the minimum Super Metroid-specific title renderer pieces listed in `BlockBox/docs/super-metroid-cinematic-port.md`. Title-sequence goal parameters: capture video and audio as MKV, emit every complete frame, do not display the Super Metroid logo or menus after the zoom-out, and keep rendering the baby Metroid cylinder with occasional spasms until the music completes or reaches its loop point. `sm.exe` also has a narrow opening-cinematic asset trace in `cinematic_assets.log`: first run showed title Mode 7 assets `kTilesTitleBgMode7`/`kTilemap_TitleMode7`, title sprite tiles, `kTiles_BabyMetroidMode7`, repeated Mode 7 tilemap updates from WRAM `7F:9000/9100/9200` to VRAM `3800`, and title sprite spritemap frames under bank `8C`. The baby Metroid title Mode 7 graphics decode cleanly as three 16x16 row-major 8x8-tile frames using palette indices 49-63 from `title-palettes.bin`; column-major and linear page interpretations are visibly wrong. The full baby capsule object is now dumped by Super Metroid-specific BlocksBox code as a generic object asset: top cap uses title Mode 7 tiles `0xBA/0xBB`, bottom cap uses `0xB8/0xB9`, and baby animation pages use offsets `0x0000/0x0100/0x0200` from `kTiles_BabyMetroidMode7`.
- BlocksBox design direction: keep it a multi-game standardized format. Game readers/dumpers can be very game-specific and should live in game-specific files, while dumped-data getters should stay generic across tilesets, object assets, levels, layers, tile placements, and object placements. BlocksBox-powered distributable software should work with user-supplied existing ROMs or disc images; ROMs are not distributed with it.
- BlocksBox generated dumps should be nested by game id under the chosen output parent. Super Metroid currently writes to `BlockBox/out/super-metroid`; future Metroid Fusion and Zero Mission work should use sibling roots such as `BlockBox/out/metroid-fusion` and `BlockBox/out/metroid-zero-mission`.
- Metroid Fusion reference sources are cloned under `other/metroid-fusion-references/`: `mf` is a fork of `metroidret/mf` on branch `codex/blockbox-fusion-export` for contribution-oriented work, while `mage-old`, `mars-patcher-py`, `mars-fusion-asm`, `OpenMFOR`, and `randovania` are local research references. For the first Fusion BlocksBox pass, treat `metroidret/mf` as the closest source-level base and MAGE as the practical editor/data-format reference.
- Super Metroid sprite dumping now uses the TooManyBees reference shape: decode 4bpp tiles, read 5-byte frame-map pieces, and composite indexed sprites around an origin with transparent color 0. Samus frames are exported under `characters/samus/frames` for power/varia/gravity suits; enemy species discovered from enemy population tables are exported under `characters/enemies/<species-name>/frames` with per-enemy manifests. SMILE remains useful as a preview/classification reference, but Samus/enemy frame PNGs should come from ROM-native graphics, palettes, and frame maps.
- BlocksBox character dumps now include visual QA sheets: Samus writes `characters/samus/samus-power-contact-sheet.png`, `samus-varia-contact-sheet.png`, and `samus-gravity-contact-sheet.png` from the first exported frame set, while enemy exports write per-enemy `frames-contact-sheet.png` files plus `characters/enemies/enemy-first-frames-contact-sheet.png` for a roster overview. Manifest fields record these paths for browsing/debug tooling.
- BlocksBox PLM/item visual pass now imports SMILE PLM reference GIFs for room-used PLM headers and nests non-door references under `objects/plms/...`, while doors remain under `objects/doors/...`. These static previews give editor visibility for stations, items, Chozo-ball items, outlets, and other placed PLMs; full ROM-native dynamic PLM reconstruction still needs per-PLM graphics/palette tracing.
- BlocksBox item organization now uses PLM instruction analysis to rename item-specific PLM folders where the script exposes a concrete item message or graphics pointer, e.g. `objects/plms/ef23-morph-ball`, `ef3f-charge-beam`, and `ef5b-varia-suit`. Generic PLM headers such as `EED7/EEDB/EEDF/EEE3` remain generic because the final item identity is placement/room-argument driven rather than fixed by the header.
- BlocksBox also dumps raw ROM-native item graphics from bank `$89` under `objects/items/<item-name>/`, with `raw-tiles.png`, two pre-palette frame PNGs, `item-graphics.json`, and `objects/items/item-graphics.tsv`. These are intentionally pre-palette graphics; colored room/editor item previews still come from PLM references until a palette-accurate item renderer is added.
- BlocksBox projectile pass now writes `objects/projectiles/projectiles.json` plus player/enemy TSV summaries. Player projectile entries currently decode bank `$93` instruction lists and spritemap pointers for beam/non-beam/SBA tables; enemy projectile entries decode bank `$86` Eproj definitions, instruction lists, and bank `$8D` spritemap pointers. Projectile PNG frames are still future work because their graphics sources depend on beam/room/enemy VRAM loads rather than a single stable tileset.
- BlocksBox now dumps ROM-native beam projectile graphics under `objects/projectiles/beam-graphics/<index-name>/`, with `raw-tiles.png`, palette-applied `palette-preview.png`, per-beam metadata, and `beam-projectile-graphics.tsv`. The beam tile pointers come from bank `$90` tables but the actual graphics DMA source is bank `$9A`; palettes remain bank `$90`. This was verified visually with `objects/projectiles/beam-graphics-contact-sheet.png`.
- Beam projectile folders also include composited projectile spritemap PNGs under `spritemaps/`, generated from bank `$93` projectile instruction spritemap pointers and the dumped beam tiles/palettes. OAM X offsets are 9-bit signed values; treating them as 16-bit signed caused impossible 512px-wide previews and has been corrected. The visual sanity sheet is `objects/projectiles/beam-spritemaps-contact-sheet.png`.
- Non-beam player projectile tables are now decoded through their damage/instruction records instead of treating table entries as instruction lists directly. BlocksBox writes missile, super missile, bomb, power bomb, shinespark echo, and SBA projectile frames under `objects/projectiles/non-beam-spritemaps/`, indexed by `non-beam-projectile-spritemaps.tsv`. Normal room startup loads `kStandardSpriteTiles` from `$9A:D200` into sprite VRAM `$6000` for `$2E00` bytes; non-beam OAM tile numbers in that range now render as ROM-native PNGs with `graphicsSourceStatus=standard-sprite-vram-0x6000`, alongside geometry preview PNGs that preserve timing/OAM words/offsets/flips/sizes.
- Enemy projectile spritemaps are now dumped under `objects/projectiles/enemy-spritemaps/`, indexed by `enemy-projectile-spritemaps.tsv`. The exporter follows bank `$86` Eproj instruction lists to bank `$8D` spritemaps and emits geometry previews for every decoded frame; it also renders frames from `kStandardSpriteTiles` when tile numbers fit base tile `0`, marked `standard-sprite-vram-0x6000-assuming-base-tile-0`. `enemy-projectile-render-variants.tsv` and each spritemap's `render-variants/` folder now apply common runtime `eproj_gfx_idx` bases (`0x0000`, `0x0200`, `0x0400`, `0x0600`, `0x0640`, `0x0A00`, `0x0C00`, `0x0E00`) as candidate previews while exact spawning enemy/room tracing remains incomplete.
- Grapple beam graphics are dumped under `objects/projectiles/grapple-beam/`: horizontal `$9A:8200`, diagonal `$9A:8A00`, and vertical `$9A:9200` tile groups, four grapple-point animation frames that DMA to sprite VRAM `$6200`, and segment previews for the angle-dependent beam chunk that DMAs to `$6210`. `grapple-beam.json` records source addresses and VRAM destinations.
- BlocksBox interface graphics are dumped under `interface/`: standard gameplay BG3 HUD/font tiles from `$9A:B200`, standard room sprite tiles from `$9A:D200`, menu BG/sprite/BG3 tile sheets from `$8E:8000`, `$B6:8000`, `$B6:C000`, and `$8E:D600`, HUD tilemap snippets, file-select/game-over menu text tilemaps, in-game message box tilemaps, pause equipment labels, pause maps, pause base tilemaps, and Samus equipment wireframes. Message boxes live under `interface/message-boxes/` with readable item/prompt names, TSV body words, body PNGs, and box previews using the native border rows. Pause labels live under `interface/pause-equipment-tilemaps/` and use pause palettes from `$B6:F000` with combined pause tiles from `$B6:8000/$B6:C000`. Pause maps live under `interface/pause-maps/`, covering seven areas with `64x32` tilemap TSV/PNG, 12-tile label TSV/PNG, raw 256-byte map-data masks, and mask preview PNGs. Pause screen bases live under `interface/pause-base-tilemaps/` from `$B6:E000/$B6:E400/$B6:E800`, and wireframes live under `interface/wireframes/` with dummy, power, varia, gravity, and varia+gravity 8x17 tilemaps; bootstrap records `interface_graphics_manifest_path` plus all interface category counts.
- BlocksBox animtiles are dumped under `animtiles/`: the exporter follows bank `$83` area animtile lists into bank `$87` animation definitions/instruction streams and emits per-frame DMA payloads, PNG previews, contact sheets, frame timing, source pointers, transfer sizes, and VRAM destinations. Current coverage includes spikes, Crateria lake/lava, Wrecked Ship screens/treadmills, Brinstar mouth tiles, Maridia sand, lava/acid/rain/spores, and Tourian statue effects. Bootstrap records `animtiles_manifest_path`, `animtiles_animation_count`, and `animtiles_frame_count`.
- BlocksBox boss/special scripted graphics are dumped under `boss-special-graphics/`: the exporter promotes boss/special `TileData` and room tileset blobs for Kraid, Phantoon, Crocomire, Torizo, Ridley, Draygon, Botwoon, Spore Spawn, Mother Brain, gunship, and Samus death graphics, and it decodes Mother Brain `ProcessSpriteTilesTransfers` tables into raw/PNG chunks with source addresses, byte counts, and VRAM destinations. Bootstrap records `boss_special_graphics_manifest_path`, `boss_special_graphics_blob_count`, `boss_special_sprite_transfer_sequence_count`, and `boss_special_sprite_transfer_chunk_count`.
- BlocksBox runtime traces are dumped under `runtime-traces/`: PLM tracing walks named PLM instruction lists and records draw frames, placement counts, item graphics load sources, candidate rotating item VRAM destinations, RAM-to-VRAM copies, item-frame commands, and eproj spawns; eproj graphics-index tracing scans the host Super Metroid source for `eproj_gfx_idx` assignments and records function/file/line plus interpreted base tile and palette when constant. Bootstrap records `runtime_trace_manifest_path`, PLM runtime counts, and `eproj_gfx_index_assignment_count`.
- BlocksBox broad graphics discovery now writes `named-graphics/` from `names.txt` labels that look like ROM tile/graphics blobs. Each entry has `raw.bin`, source metadata, and automatic 2bpp/4bpp preview sheets when aligned; likely compressed tile labels also get `decompressed.bin` plus decompressed preview sheets when the Super Metroid decompressor succeeds. Bootstrap records `named_graphics_manifest_path` and `named_graphics_count`. Treat this as an inventory and source-byte preservation pass, not final semantic decoding: some entries are compressed graphics, interleaved Mode 7 data, animation scripts, or pointer tables that need game-specific decoders in later rounds.
- ComfyUI pixel/sprite upscaling support is installed under `F:\AI\ComfyUI_windows_portable\ComfyUI\user\default\workflows\SuperMetroid-PixelSprite-Upscale`. The preferred Samus sprite look so far is PixelPerfectV4 4x, downscaled to exact 2x, then remapped to the original sprite palette using sRGB nearest-color matching; OKLab was tested but looked less faithful for this palette. Use nearest-neighbor plus palette quantization for stricter editable pixel art, and reserve the low-denoise Ultimate SD Upscale + SD1.5 tile ControlNet workflow for intentional detail reinterpretation.

## Update Guidance

- Keep this file high level.
- Update this file whenever the user sets a new goal, completes a goal, pauses work, or meaningfully changes direction.
- Add or revise goals when the user changes direction, and remove or reword stale goals so the active intent stays accurate.
- When work is completed, record the outcome briefly or move the goal out of the active list instead of leaving future agents to rediscover its status.
- Avoid turning this into a changelog; focus on active intent and collaborator preferences.
